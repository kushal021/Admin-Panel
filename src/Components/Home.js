import React from 'react';
import { collection, getDocs, doc, deleteDoc, updateDoc } from "firebase/firestore";

const Home = () => {
    return (
        <>
            
        </>
    )
};

export default Home;
